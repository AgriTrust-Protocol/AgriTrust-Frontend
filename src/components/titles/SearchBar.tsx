"use client";
import { useMemo, useState } from "react";
import type { LandTitle } from "./titleTypes";
export function SearchBar({ parcels, onSelect }: { parcels: LandTitle[]; onSelect: (id: string) => void }) { const [query, setQuery] = useState(""); const results = useMemo(() => { const value = query.toLowerCase().trim(); return value ? parcels.filter((p) => [p.parcelId,p.owner,p.location,p.name].some((v) => v.toLowerCase().includes(value))).slice(0, 5) : []; }, [parcels, query]); return <div className="relative"><label className="sr-only" htmlFor="parcel-search">Search property records</label><input id="parcel-search" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search parcel ID, owner address, or location" className="w-full rounded-xl border border-zinc-300 bg-white px-4 py-3 text-sm outline-none focus:border-emerald-600" />{results.length > 0 && <ul className="absolute z-30 mt-2 w-full overflow-hidden rounded-xl border border-zinc-200 bg-white shadow-lg">{results.map((parcel) => <li key={parcel.tokenId}><button onClick={() => { onSelect(parcel.tokenId); setQuery(""); }} className="w-full px-4 py-3 text-left hover:bg-emerald-50"><span className="block text-sm font-semibold text-zinc-800">{parcel.name}</span><span className="text-xs text-zinc-500">{parcel.parcelId} · {parcel.location}</span></button></li>)}</ul>}</div>; }
